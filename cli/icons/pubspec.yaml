name: icons
description: Export all icons from all Widgets in `lib` library to a HTML file.
version: 1.0.0

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
   html: ^0.15.0

dev_dependencies:
  pedantic: ^1.10.0
